rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore access goes through server-side Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
